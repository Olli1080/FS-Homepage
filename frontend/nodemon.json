{
    "watch": ["src"],
    "ext": "ts,json,js",
    "ignore": ["src/**/*.spec.ts"],
    "exec": "node --experimental-specifier-resolution=node --loader ts-paths-esm-loader ./src/server/entry.ts",
    "env": {
        "NODE_ENV": "development"
    }
  }